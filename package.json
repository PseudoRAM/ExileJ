{
    "name": "exilej",
    "displayName": "ExileJ",
    "description": "Compiles and Executes Java",
    "version": "0.0.1",
    "publisher": "PseudoRAM",
    "engines": {
        "vscode": "^1.0.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:extension.startExile"
    ],
    "main": "./extension",
    "contributes": {
        "commands": [{
            "command": "extension.startExile",
            "title": "ExileJ"
        }]
    },
    "scripts": {
        "postinstall": "node ./node_modules/vscode/bin/install"
    },
    "devDependencies": {
        "vscode": "^0.11.0"
    }
}